<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jiecxy.security"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 读取sd卡权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- 检测拨号权限 -->
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/>

    <!-- 获取手机信息 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <!-- 读取联系人 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 这个是用模拟器进行测试才需要用到的权限，表示是这个模拟的位置
     <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"/> -->


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:theme="@android:style/Theme.NoTitleBar"
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".ui.LostProtectedActivity"
            android:label="@string/protecte" />

        <activity
            android:name=".ui.SetupGuide1.SetupGuide1Activity"
            android:label="@string/protecte" />

        <activity
            android:name=".ui.SetupGuide2.SetupGuide2Activity"
            android:label="@string/protecte" />

        <activity
            android:name=".ui.SetupGuide3.SetupGuide3Activity"
            android:label="@string/protecte" />

        <activity
            android:name=".ui.SetupGuide4.SetupGuide4Activity"
            android:label="@string/protecte" />

        <activity
            android:name=".ui.SelectContactActivity"
            android:label="@string/protecte" />

        <activity
            android:name=".ui.AToolActivity"
            android:label="@string/atool" />

        <activity
            android:name=".ui.QueryNumberActivity"
            android:label="@string/queryNumber" />

        <receiver
            android:name=".receiver.CallPhoneReceiver">
            <intent-filter android:priority="1000"><!-- 把优先级设置高一些，以便第一个拿到广播 -->
                <action android:name="android.intent.action.NEW_OUTGOING_CALL"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.jiecxy.security.receiver.BootCompleteReceiver">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED"/><!-- 这个是开机完成后的广播 -->
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.jiecxy.security.receiver.SmsReceiver">
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" /><!-- 获取短信内容 -->
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.jiecxy.security.receiver.MyAdminReceiver">
            <meta-data android:name="android.app.device_admin"
                android:resource="@xml/myadmin" />  <!-- meta-data 指定 xml文件 -->
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
